# What did you do? 
- I download Git
- I create account on GitHub
- I login git on Windows
- Create repository on GitHub
- Make a folder including the web exercises that we did
- change the directory to the folder web exercises
- Initialized empty Git repository in web exercises folder using command
$ git init
- add all the folders using this command $ git add .
- $ git commit -m "firs commit"
- $ git commit --amend
- $ $ git branch -M main
- $ git remote add origin https://github.com/BasmaAlomari/webEx.git
- $ git push -u origin main

## What technologies did you use? 
- HTML
-CSS

## What did you learn?
 HTML and CSS from W3schools and how to use git to push the code 
